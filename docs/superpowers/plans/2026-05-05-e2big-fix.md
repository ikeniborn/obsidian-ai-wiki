# E2BIG Fix: temp files for large CLI prompts

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Устранить `Error: spawn E2BIG` при lint/fix, заменив передачу большого контента через argv на временные файлы.

**Architecture:** В `_create()` размер `userText` и `systemContent` проверяется против порога 32 KB. Большой контент записывается во временный файл в `<plugin>/tmp/`, путь передаётся через `--append-system-prompt-file` / `--system-prompt-file`. Пути temp-файлов передаются в `_generate()` для удаления в `finally`.

**Tech Stack:** TypeScript, Node.js `node:fs` (sync), `node:path`, vitest, Obsidian Plugin API.

---

## Файлы

| Файл | Изменение |
|---|---|
| `src/claude-cli-client.ts` | Добавить импорты `node:fs`+`node:path`; `tmpDir` в `ClaudeCliConfig`; логика temp-файлов в `_create()`; `tmpFiles` в сигнатурах `_makeIterable`/`_collect`/`_generate`; очистка в `finally` |
| `src/controller.ts` | Вычислить `tmpDir` через `manifest.dir` + `getFullPath`; передать в `ClaudeCliClient` |
| `tests/claude-cli-client.test.ts` | Добавить `tmpDir` в тестовый cfg; добавить тесты для большого `userText` и `systemContent` |

---

### Task 1: Тесты для temp-file поведения

**Files:**
- Modify: `tests/claude-cli-client.test.ts`

- [ ] **Шаг 1.1: Добавить мок `node:fs` в начало файла**

Открыть `tests/claude-cli-client.test.ts`. Добавить сразу после существующих `vi.mock` (после строки 9):

```typescript
vi.mock("node:fs", () => ({
  mkdirSync: vi.fn(),
  writeFileSync: vi.fn(),
  unlinkSync: vi.fn(),
}));

import { mkdirSync, writeFileSync, unlinkSync } from "node:fs";
```

- [ ] **Шаг 1.2: Добавить `tmpDir` в тестовый `cfg` (строка 33)**

Заменить:
```typescript
const cfg = { iclaudePath: "/usr/bin/claude", model: "sonnet", maxTokens: 1024, requestTimeoutSec: 30 };
```
На:
```typescript
const cfg = { iclaudePath: "/usr/bin/claude", model: "sonnet", maxTokens: 1024, requestTimeoutSec: 30, tmpDir: "/plugin/tmp" };
```

- [ ] **Шаг 1.3: Написать тест — большой `userText` → temp-файл**

Добавить в `describe("ClaudeCliClient", ...)` после существующих тестов:

```typescript
it("uses --append-system-prompt-file when userText exceeds 32KB", async () => {
  (spawn as any).mockReturnValue(makeMockProcess([]));

  const client = new ClaudeCliClient(cfg);
  const largeText = "x".repeat(33_000); // > 32 768 байт

  await client.chat.completions.create(
    { model: "sonnet", messages: [{ role: "user", content: largeText }], stream: false } as any,
  );

  // spawn получил --append-system-prompt-file и -p "."
  const args: string[] = (spawn as any).mock.calls[0][1];
  expect(args).toContain("--append-system-prompt-file");
  expect(args).toContain(".");
  const pIdx = args.indexOf("-p");
  expect(args[pIdx + 1]).toBe(".");

  // файл записан
  expect(writeFileSync).toHaveBeenCalledWith(
    expect.stringContaining("llm-wiki-usr-"),
    largeText,
    "utf-8",
  );

  // файл удалён после завершения
  expect(unlinkSync).toHaveBeenCalledWith(
    expect.stringContaining("llm-wiki-usr-"),
  );
});
```

- [ ] **Шаг 1.4: Написать тест — большой `systemContent` → `--system-prompt-file`**

```typescript
it("uses --system-prompt-file when systemContent exceeds 32KB", async () => {
  (spawn as any).mockReturnValue(makeMockProcess([]));

  const client = new ClaudeCliClient(cfg);
  const largeSystem = "s".repeat(33_000);

  await client.chat.completions.create(
    {
      model: "sonnet",
      messages: [
        { role: "system", content: largeSystem },
        { role: "user", content: "hi" },
      ],
      stream: false,
    } as any,
  );

  const args: string[] = (spawn as any).mock.calls[0][1];
  expect(args).toContain("--system-prompt-file");
  expect(args).not.toContain("--system-prompt");

  expect(writeFileSync).toHaveBeenCalledWith(
    expect.stringContaining("llm-wiki-sys-"),
    largeSystem,
    "utf-8",
  );

  expect(unlinkSync).toHaveBeenCalledWith(
    expect.stringContaining("llm-wiki-sys-"),
  );
});
```

- [ ] **Шаг 1.5: Написать тест — малый контент остаётся в argv**

```typescript
it("keeps small userText and systemContent inline in argv", async () => {
  (spawn as any).mockReturnValue(makeMockProcess([]));

  const client = new ClaudeCliClient(cfg);

  await client.chat.completions.create(
    {
      model: "sonnet",
      messages: [
        { role: "system", content: "be helpful" },
        { role: "user", content: "short question" },
      ],
      stream: false,
    } as any,
  );

  const args: string[] = (spawn as any).mock.calls[0][1];
  // inline -p с настоящим контентом
  const pIdx = args.indexOf("-p");
  expect(args[pIdx + 1]).toBe("short question");
  // inline --system-prompt
  expect(args).toContain("--system-prompt");
  expect(args).not.toContain("--system-prompt-file");
  expect(args).not.toContain("--append-system-prompt-file");
  // fs не вызывался
  expect(writeFileSync).not.toHaveBeenCalled();
});
```

- [ ] **Шаг 1.6: Запустить тесты — убедиться в базовом состоянии**

```bash
npx vitest run tests/claude-cli-client.test.ts
```

Ожидаемый результат:
- Тест 1.3 (большой `userText`) — **FAIL**: `--append-system-prompt-file` нет в args
- Тест 1.4 (большой `systemContent`) — **FAIL**: `--system-prompt-file` нет в args
- Тест 1.5 (малый контент) — **PASS**: текущий код уже передаёт `-p` инлайн и не вызывает `writeFileSync`
- Все ранее существовавшие тесты — **PASS**

---

### Task 2: Реализация в `claude-cli-client.ts`

**Files:**
- Modify: `src/claude-cli-client.ts`

- [ ] **Шаг 2.1: Добавить импорты**

В начало файла после существующих импортов (после строки 4) добавить:

```typescript
import { mkdirSync, writeFileSync, unlinkSync } from "node:fs";
import { join } from "node:path";
```

- [ ] **Шаг 2.2: Добавить `tmpDir` в `ClaudeCliConfig`**

Найти интерфейс `ClaudeCliConfig` (строка 7–13) и добавить поле:

```typescript
export interface ClaudeCliConfig {
  iclaudePath: string;
  model: string;
  requestTimeoutSec: number;
  cwd?: string;
  allowedTools?: string;
  tmpDir: string;
}
```

- [ ] **Шаг 2.3: Переписать `_create()` с temp-file логикой**

Заменить тело метода `_create()` (строки 32–59) целиком:

```typescript
private _create(
  params:
    | OpenAI.Chat.ChatCompletionCreateParamsStreaming
    | OpenAI.Chat.ChatCompletionCreateParamsNonStreaming,
  opts?: { signal?: AbortSignal },
): Promise<AsyncIterable<OpenAI.Chat.ChatCompletionChunk> | OpenAI.Chat.ChatCompletion> {
  const messages = params.messages;
  const systemContent = messages
    .filter((m) => m.role === "system")
    .map((m) => (typeof m.content === "string" ? m.content : ""))
    .join("\n\n");
  const lastUser = [...messages].reverse().find((m) => m.role === "user");
  const userText = typeof lastUser?.content === "string" ? lastUser.content : "";

  const model = (params as { model?: string }).model || this.cfg.model;
  const { requestTimeoutSec } = this.cfg;

  const LARGE_THRESHOLD = 32_768;
  const id = `${Date.now()}-${Math.random().toString(36).slice(2)}`;
  const tmpFiles: string[] = [];

  const args: string[] = [];
  if (model) args.push("--model", model);
  args.push("--");

  try {
    const isLargeUser = Buffer.byteLength(userText, "utf8") > LARGE_THRESHOLD;
    if (isLargeUser) {
      mkdirSync(this.cfg.tmpDir, { recursive: true });
      const tmpUsrFile = join(this.cfg.tmpDir, `llm-wiki-usr-${id}.txt`);
      writeFileSync(tmpUsrFile, userText, "utf-8");
      tmpFiles.push(tmpUsrFile);
      args.push("-p", ".");
      args.push("--append-system-prompt-file", tmpUsrFile);
    } else {
      args.push("-p", userText);
    }

    args.push("--output-format", "stream-json", "--verbose");
    args.push("--disable-slash-commands");
    args.push("--dangerously-skip-permissions");

    if (this.cfg.allowedTools) args.push("--tools", this.cfg.allowedTools);

    if (systemContent) {
      const isLargeSys = Buffer.byteLength(systemContent, "utf8") > LARGE_THRESHOLD;
      if (isLargeSys) {
        mkdirSync(this.cfg.tmpDir, { recursive: true });
        const tmpSysFile = join(this.cfg.tmpDir, `llm-wiki-sys-${id}.txt`);
        writeFileSync(tmpSysFile, systemContent, "utf-8");
        tmpFiles.push(tmpSysFile);
        args.push("--system-prompt-file", tmpSysFile);
      } else {
        args.push("--system-prompt", systemContent);
      }
    }
  } catch (err) {
    for (const f of tmpFiles) { try { unlinkSync(f); } catch { /* ignore */ } }
    throw err;
  }

  if ((params as { stream?: boolean }).stream) {
    return Promise.resolve(this._makeIterable(args, opts?.signal, requestTimeoutSec, tmpFiles));
  }
  return this._collect(args, opts?.signal, requestTimeoutSec, tmpFiles);
}
```

- [ ] **Шаг 2.4: Обновить сигнатуру и тело `_makeIterable()`**

Заменить метод `_makeIterable` (строки 61–67):

```typescript
private _makeIterable(
  args: string[],
  signal: AbortSignal | undefined,
  timeoutSec: number,
  tmpFiles: string[],
): AsyncIterable<OpenAI.Chat.ChatCompletionChunk> {
  return { [Symbol.asyncIterator]: () => this._generate(args, signal, timeoutSec, tmpFiles) };
}
```

- [ ] **Шаг 2.5: Обновить сигнатуру `_generate()` и добавить очистку в `finally`**

Заменить строку объявления `_generate` (строка 69):
```typescript
private async *_generate(
  args: string[],
  signal: AbortSignal | undefined,
  timeoutSec: number,
  tmpFiles: string[],
): AsyncGenerator<OpenAI.Chat.ChatCompletionChunk> {
```

В существующем `finally`-блоке `_generate()` (после `rl.close()`) добавить строку очистки:

```typescript
} finally {
  clearTimeout(timeoutHandle);
  signal?.removeEventListener("abort", onAbort);
  rl.close();
  for (const f of tmpFiles) { try { unlinkSync(f); } catch { /* already gone */ } }
  if (child.exitCode === null) {
    child.kill("SIGTERM");
    setTimeout(() => { if (child.exitCode === null) child.kill("SIGKILL"); }, SIGTERM_GRACE_MS);
  }
}
```

- [ ] **Шаг 2.6: Обновить сигнатуру `_collect()`**

Заменить строку объявления `_collect` (строка 151):
```typescript
private async _collect(
  args: string[],
  signal: AbortSignal | undefined,
  timeoutSec: number,
  tmpFiles: string[],
): Promise<OpenAI.Chat.ChatCompletion> {
  let text = "";
  for await (const chunk of this._generate(args, signal, timeoutSec, tmpFiles)) {
    text += (chunk.choices[0]?.delta as { content?: string })?.content ?? "";
  }
  return {
    id: "cc-0",
    object: "chat.completion",
    model: this.cfg.model || "claude",
    created: 0,
    choices: [{ index: 0, message: { role: "assistant", content: text }, finish_reason: "stop", logprobs: null }],
    usage: { prompt_tokens: 0, completion_tokens: 0, total_tokens: 0 },
  };
}
```

- [ ] **Шаг 2.7: Запустить тесты — убедиться что все проходят**

```bash
npx vitest run tests/claude-cli-client.test.ts
```

Ожидаемый результат: все тесты PASS (включая новые из Task 1).

- [ ] **Шаг 2.8: Проверить TypeScript компилятор**

```bash
npm run build 2>&1 | head -30
```

Ожидаемый результат: сборка без ошибок TypeScript. Если есть ошибки — исправить перед продолжением.

- [ ] **Шаг 2.9: Закоммитить**

```bash
git add src/claude-cli-client.ts tests/claude-cli-client.test.ts
git commit -m "fix(claude-cli-client): use temp files for large prompts to avoid E2BIG"
```

---

### Task 3: Обновить `controller.ts` — передать `tmpDir`

**Files:**
- Modify: `src/controller.ts`

- [ ] **Шаг 3.1: Вычислить `pluginDir` и `tmpDir` в `buildAgentRunner()`**

Найти метод `buildAgentRunner()` (строка 168). Вставить три строки сразу после строки 170 (`const base = ...`), перед строкой 171 (`const vaultTools = ...`):

```typescript
  // вставить между строками "const base = ..." и "const vaultTools = ..."
  const manifestDir = this.plugin.manifest.dir
    ?? join(this.app.vault.configDir, "plugins", this.plugin.manifest.id);
  const pluginDir = (this.app.vault.adapter as { getFullPath: (p: string) => string })
    .getFullPath(manifestDir);
  const tmpDir = join(pluginDir, "tmp");
```

`join` уже импортирован в `controller.ts` (строка 3: `import { relative, isAbsolute, join } from "node:path"`), новый импорт не нужен.

- [ ] **Шаг 3.2: Передать `tmpDir` в `ClaudeCliClient`**

Найти строку создания `ClaudeCliClient` (строка 178). Добавить `tmpDir` в конец объекта:

```typescript
// до:
new ClaudeCliClient({ ...s.claudeAgent, requestTimeoutSec: maxTimeoutSec, cwd: s.claudeAgent.spawnCwd || "/tmp" })

// после:
new ClaudeCliClient({ ...s.claudeAgent, requestTimeoutSec: maxTimeoutSec, cwd: s.claudeAgent.spawnCwd || "/tmp", tmpDir })
```

Строку с `new OpenAI(...)` не трогать.

- [ ] **Шаг 3.3: Проверить сборку**

```bash
npm run build 2>&1 | head -30
```

Ожидаемый результат: сборка без ошибок. Ошибка вида `Property 'tmpDir' is missing` означает что шаг 2.2 не был применён.

- [ ] **Шаг 3.4: Запустить все тесты**

```bash
npm test
```

Ожидаемый результат: все тесты PASS. Если что-то упало — исправить перед коммитом.

- [ ] **Шаг 3.5: Закоммитить**

```bash
git add src/controller.ts
git commit -m "fix(controller): pass plugin tmpDir to ClaudeCliClient"
```
