# README: ценностное предложение + ссылка на docs/dev.md

**Дата:** 2026-05-06  
**Аудитория:** публичный GitHub-репозиторий  
**Объём:** ~10 строк добавляем, 1 строку меняем

---

## Цель

1. Добавить ценностное предложение в начало README — чтобы незнакомый с плагином GitHub-зритель понял «зачем» за 5 секунд.
2. Сделать ссылку на `docs/dev.md` более заметной — заменить одиночный blockquote в конце на секцию `## Документация` со списком ссылок.

---

## Изменения

### 1. Ценностный блок (вставить после строки 3, перед `> Поддерживаемые бэкенды`)

```markdown
Превращает необработанные заметки в структурированную wiki — локально, без облака, без подписок.

**Почему LLM Wiki:**
- **Офлайн по умолчанию** — Ollama или любой OpenAI-compatible сервер; данные не покидают машину
- **Компаундируется** — каждый Ingest обогащает базу; связи и страницы накапливаются сами
- **Прозрачность** — прогресс шагов агента виден в реальном времени в боковой панели
- **Два бэкенда** — Native Agent (Ollama / OpenAI) и Claude Code; переключаются в настройках
```

**Позиция:** после строки `Автоматически строит и пополняет wiki-базу знаний из ваших заметок с помощью LLM.`, перед строкой `> Поддерживаемые бэкенды`.

---

### 2. Секция документации (заменяет последнюю строку README)

**Удалить:**
```markdown
> Инструкции для разработчиков, сборка и smoke-test чеклист — в [docs/dev.md](docs/dev.md).
```

**Добавить:**
```markdown
## Документация

- [docs/dev.md](docs/dev.md) — сборка, установка, smoke-test чеклист для разработчиков
- [docs/publishing.md](docs/publishing.md) — публикация релиза
```

---

## Что не меняем

- Секции «Что умеет», «Быстрый старт», «Команды», «Справочник настроек» — без изменений.
- Стиль и язык README — русский, технический, без маркетингового пафоса.

---

## Критерии готовности

- `npm run build` проходит (README не влияет на сборку, проверка для порядка).
- README открывается на GitHub и ценностный блок виден без скролла.
- Ссылки `docs/dev.md` и `docs/publishing.md` кликабельны и ведут на существующие файлы.
