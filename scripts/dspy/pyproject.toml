[project]
name = "dspy-optimizer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "dspy-ai>=2.5",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
